import { Screen } from '@components';
import { Button } from '@ui-kitten/components';

const Login = () => {
	return (
		<Screen level="1">
			<Button size="giant" disabled>
				BUTTON
			</Button>
		</Screen>
	);
};

export default Login;
